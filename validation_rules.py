put_recipe_rules = {
    'name': 'required|string', # required name, alphabetic chars only
    'ingredients': 'required|list', # required ingredients, must be list
    'instructions': 'required|string' # required instructions, string
}